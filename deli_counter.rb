katz_deli = []

def line(line_array)
  line_length = line_array.length
    if line_length = 0
      puts "The line is currently empty."
    else
      puts "The line is currently #{index}"
    end
end
